---
name: kujo-dependency-scanner
description: Use when a cheap KUJO worker should run explicit dependency, package, manifest, lockfile, Kennel, or Scout status checks.
---

# Dependency Scanner Skill

Read `AGENT.md` first. Run read-only dependency checks unless changes are explicitly authorized.
