#!/usr/bin/env python3
"""Run repository-owned VideoOps tools without a sibling runtime checkout."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from videoops.cli import main
raise SystemExit(main())
